<Window x:Class="AirStudio.CommercialManager.Windows.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#0D1B2A">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptText" Text="Enter value:"
                   Foreground="#B0C4DE" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <TextBox Grid.Row="1" x:Name="InputTextBox"
                 Height="28" Padding="4,2"
                 Background="#1A2A40" Foreground="#FFFFFF"
                 BorderBrush="#3D5A80" BorderThickness="1"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Click="OK_Click" Width="80" IsDefault="True"
                    Background="#3D5A80" Foreground="#FFFFFF" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80" IsCancel="True"
                    Background="#1A2A40" Foreground="#FFFFFF"/>
        </StackPanel>
    </Grid>
</Window>
