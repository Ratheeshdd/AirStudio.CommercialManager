<Window x:Class="AirStudio.CommercialManager.Windows.LibraryCleanupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Library Cleanup"
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        Loaded="Window_Loaded">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="24" VerticalAlignment="Center"
                       Foreground="#5A8FC4" Margin="0,0,12,0"/>
            <StackPanel>
                <TextBlock Text="LIBRARY CLEANUP" FontWeight="Bold" FontSize="18" Foreground="#B0C4DE"/>
                <TextBlock x:Name="ChannelLabel" Text="Channel: --" FontSize="12" Foreground="#808080"/>
            </StackPanel>
        </StackPanel>

        <!-- Year and Status Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,16,0">
                <TextBlock Text="Year:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="YearComboBox" Width="100" SelectionChanged="YearComboBox_SelectionChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0">
                <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="StatusComboBox" Width="120" SelectionChanged="StatusComboBox_SelectionChanged">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Active Only"/>
                </ComboBox>
            </StackPanel>

            <Button Grid.Column="3" Content="&#xE72C; Refresh" Click="RefreshButton_Click" Padding="12,4"/>
        </Grid>

        <!-- Select All and Count -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0" x:Name="SelectAllCheckBox" Content="Select All"
                      Click="SelectAllCheckBox_Click" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="CountLabel" Text="(0 items)"
                       Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#808080"/>
        </Grid>

        <!-- Commercials DataGrid -->
        <DataGrid Grid.Row="3" x:Name="CommercialsDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#3A4A5A">

            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="30">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Click="ItemCheckBox_Click" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Spot Name" Binding="{Binding Spot}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="Agency" Binding="{Binding Agency}" Width="150"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="80"/>
                <DataGridTextColumn Header="Created" Binding="{Binding DateIn, StringFormat='{}{0:dd-MMM-yy}'}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Selection Summary -->
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock x:Name="SelectionSummaryLabel" Text="Selected: 0 commercials"
                           VerticalAlignment="Center" FontWeight="SemiBold"/>
                <TextBlock x:Name="SizeSummaryLabel" Text="" Margin="16,0,0,0"
                           VerticalAlignment="Center" Foreground="#808080"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="MoveToTrashButton" Content="Move to Trash"
                    Click="MoveToTrashButton_Click" IsEnabled="False"
                    Style="{StaticResource PrimaryButtonStyle}" Width="130" Margin="0,0,8,0"/>
            <Button Grid.Column="2" x:Name="ViewTrashButton" Content="View Trash"
                    Click="ViewTrashButton_Click" Width="100"/>
        </Grid>

        <!-- Warning and Close -->
        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                           VerticalAlignment="Center" Foreground="#FFA500" Margin="0,0,8,0"/>
                <TextBlock Text="Items in trash are permanently deleted after 30 days"
                           VerticalAlignment="Center" Foreground="#B0B0B0" FontStyle="Italic"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Close" Click="CloseButton_Click"
                    Width="100" IsCancel="True"/>
        </Grid>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.Row="0" Grid.RowSpan="6"
                Background="#80000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading commercials..." Foreground="White" FontSize="14"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
