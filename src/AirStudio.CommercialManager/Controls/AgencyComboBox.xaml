<UserControl x:Class="AirStudio.CommercialManager.Controls.AgencyComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="36" d:DesignWidth="300">

    <Grid>
        <ComboBox x:Name="AgencyCombo"
                  IsEditable="True"
                  IsTextSearchEnabled="False"
                  StaysOpenOnEdit="True"
                  DisplayMemberPath="AgencyName"
                  SelectedValuePath="Code"
                  Height="36"
                  PreviewTextInput="AgencyCombo_PreviewTextInput"
                  SelectionChanged="AgencyCombo_SelectionChanged"
                  DropDownClosed="AgencyCombo_DropDownClosed"
                  KeyDown="AgencyCombo_KeyDown">
            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Padding" Value="8,6"/>
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>
    </Grid>
</UserControl>
