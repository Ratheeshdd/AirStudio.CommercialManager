<UserControl x:Class="AirStudio.CommercialManager.Controls.WaveformViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="600"
             Background="#0D1B2A">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Waveform Display Area -->
        <Border Grid.Row="0" CornerRadius="4" ClipToBounds="True"
                MouseLeftButtonDown="WaveformArea_MouseLeftButtonDown">
            <Grid>
                <!-- Waveform Canvas -->
                <Canvas x:Name="WaveformCanvas" Background="Transparent"/>

                <!-- Playhead -->
                <Canvas x:Name="PlayheadCanvas">
                    <Line x:Name="Playhead" Y1="0" Y2="{Binding ActualHeight, ElementName=WaveformCanvas}"
                          Stroke="#FF6B6B" StrokeThickness="2" Visibility="Collapsed"/>
                </Canvas>

                <!-- Loading Overlay -->
                <Border x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Loading waveform..." Foreground="#B0C4DE" FontSize="12"/>
                    </StackPanel>
                </Border>

                <!-- Empty State -->
                <Border x:Name="EmptyOverlay" Background="Transparent">
                    <TextBlock Text="Select an audio file to view waveform"
                               Foreground="#607D8B" FontSize="12" FontStyle="Italic"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>

        <!-- Time Display -->
        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="CurrentTimeLabel" Text="00:00:00"
                       Foreground="#B0C4DE" FontSize="11" FontFamily="Consolas"/>

            <Slider Grid.Column="1" x:Name="PositionSlider" Margin="12,0"
                    Minimum="0" Maximum="100" Value="0"
                    ValueChanged="PositionSlider_ValueChanged"
                    PreviewMouseDown="PositionSlider_PreviewMouseDown"
                    PreviewMouseUp="PositionSlider_PreviewMouseUp"
                    IsEnabled="False"/>

            <TextBlock Grid.Column="2" x:Name="TotalTimeLabel" Text="00:00:00"
                       Foreground="#B0C4DE" FontSize="11" FontFamily="Consolas"/>
        </Grid>

        <!-- Playback Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button x:Name="PlayPauseButton" Content="▶" Width="40" Height="30"
                    Click="PlayPauseButton_Click" IsEnabled="False"
                    ToolTip="Play/Pause"/>
            <Button x:Name="StopButton" Content="■" Width="40" Height="30"
                    Click="StopButton_Click" IsEnabled="False" Margin="4,0,0,0"
                    ToolTip="Stop"/>

            <!-- Segment Info (for capsules) -->
            <Border x:Name="SegmentInfo" Margin="20,0,0,0" Padding="8,4" CornerRadius="4"
                    Background="#1A2A40" Visibility="Collapsed">
                <TextBlock x:Name="SegmentLabel" Text="Segment: --"
                           Foreground="#B0C4DE" FontSize="11"/>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
